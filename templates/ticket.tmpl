{{template "header" .}}

<div class="container">
  <div class="row">
    {{if eq .userPermissions.CanCreateContacts "true"}}
    <br>
      <div class="margin-md">
        <br>
        <button type="button" onclick="makeEditable()" id="editItemBtn" class="btn btn-primary">Edit</button> 
        <button type="button" onclick="saveChanges('Customer')" style="display: none;" id="saveChangesBtn" class="btn btn-primary">Save</button>
        <button id="myBtn" class="btn btn-danger" onclick="showRemoveModal()">Remove</button>
        <br>
    
     </div>
     <br>
     {{end}}
         <h1 class="editable" id="name">{{.name}}</h1>
         {{.description}}

  </div>
  <div class="row">
    <p>ID: <span id="objectId">{{.ticket_id}}</span></p>
    <p>Product: <span id="product">{{.product_name}}</span></p>
    <p>Customer: <span id="customer">{{.customer_name}}</span></p>
    <p>Contact: <span id="contact">{{.contact_name}}</span></p>
  </div>
</div>



  <script>


    
  </script>
    {{template "footer" .}}
